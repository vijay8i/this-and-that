cflags = []
cflags += [ "-std=c++23" ]

shared_library("ss-shared") {
  sources = [
    "simple-stats.cc",
    "simple-stats.hh",
  ]

  defines = [ "STATS_SHARED_LIBRARY" ]
}

static_library("ss-static") {
  sources = [
    "simple-stats.cc",
    "simple-stats.hh",
  ]
}
